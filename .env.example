# Token for incoming requests to this proxy
LITE_TOKEN=sk-your-lite-proxy-token

# Upstream 9Router token (with "Bearer " prefix or "Bearer " appended by the proxy)
UPSTREAM_TOKEN=Bearer sk-your-9router-token
